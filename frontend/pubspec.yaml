name: trustguard_ai
description: TrustGuard AI — Real-Time Fraud Shield for ASEAN Digital Wallets
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  fl_chart: ^0.66.0
  animate_do: ^3.3.4
  flutter_animate: ^4.3.0
  provider: ^6.1.1
  intl: ^0.19.0
  percent_indicator: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
